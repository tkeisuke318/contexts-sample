- このドキュメントはデータ移行仕様書.mdのテンプレートです。
- ★★または> ★★ で始まる文章とその周辺は、このドキュメントを作成する際の指示文のため、指示として受け止め、最終成果物には残さないでください。

# データ移行仕様書

---

## ドキュメント情報

> ★★ このドキュメントの管理情報（ID・日付・作成者・承認者）を記入する

| 項目 | 内容 |
|------|------|
| ドキュメントID | MIG-DATA-[連番4桁] |
| プロジェクト名 | ★★プロジェクト名 |
| 作成日 | ★★YYYY-MM-DD |
| 作成者 | ★★氏名 |
| 版数 | 1.0 |

---

## テーブルマッピング一覧

> ★★ 移行元テーブルと移行先テーブルの対応関係・件数・変換有無を一覧化する

| # | 移行元テーブル（旧） | 移行先テーブル（新） | 件数（想定） | 変換処理 |
|---|------------------|------------------|-----------|---------|
| 1 | ★★旧テーブル名 | ★★新テーブル名 | ★★○件 | ★★そのまま / 変換あり |

---

## テーブル別カラムマッピング

> ★★ テーブルごとにカラムの対応・変換ルール・NULL時の扱いと移行SQLを定義する

### ★★旧テーブル名 → ★★新テーブル名

| # | 移行元カラム名 | 移行先カラム名 | 変換ルール | NULL時の扱い |
|---|-------------|-------------|---------|------------|
| 1 | ★★old_column | ★★new_column | ★★そのまま / 変換内容（例：'0'→false, '1'→true） | ★★NULLのまま / デフォルト値○○ |

#### 移行SQL

```sql
-- ★★テーブル名 移行SQL
INSERT INTO ★★新テーブル名 (
    ★★new_column1,
    ★★new_column2,
    created_at,
    updated_at
)
SELECT
    ★★old_column1,
    ★★変換式（例：CASE WHEN old_status = '1' THEN 'ACTIVE' ELSE 'INACTIVE' END）,
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
FROM
    ★★旧テーブル名
WHERE
    ★★移行条件（例：deleted_flag = '0'）;
```

---

## データ検証仕様

> ★★ 移行後のデータ正確性を確認するための件数・合計値・サンプル検証のSQLを定義する

### 件数検証

```sql
-- 移行前件数
SELECT COUNT(*) AS before_count FROM ★★旧テーブル名 WHERE ★★移行条件;

-- 移行後件数
SELECT COUNT(*) AS after_count FROM ★★新テーブル名;

-- 件数が一致することを確認する
```

### 合計値検証（金額・数量系テーブルの場合）

```sql
-- 移行前合計
SELECT SUM(★★金額カラム) AS before_sum FROM ★★旧テーブル名 WHERE ★★移行条件;

-- 移行後合計
SELECT SUM(★★金額カラム) AS after_sum FROM ★★新テーブル名;
```

### サンプル検証

| # | 検証対象 | 検証内容 | 確認方法 |
|---|---------|---------|---------|
| 1 | ★★重要データ（例：最新受注データ） | ★★移行前後で値が一致すること | ★★SQLで比較 / 画面で目視確認 |

---

## 文字コード・データ型変換

> ★★ 文字コード・日付形式・フラグ値などの変換ルールを旧システムと新システムの対比で記述する

| 項目 | 旧システム | 新システム | 変換処理 |
|------|----------|----------|---------|
| 文字コード | ★★Shift-JIS | ★★UTF-8 | ★★変換ツール名 or iconvコマンド |
| 日付形式 | ★★YYYYMMDD | ★★YYYY-MM-DD | ★★変換SQL（例：STR_TO_DATE） |
| フラグ値 | ★★'0'/'1' | ★★false/true | ★★CASE WHEN変換 |

---

## 変更履歴

> ★★ ドキュメントの改版履歴を記録する。初版作成時は版数1.0、変更内容に「初版作成」と記入する

| 版数 | 変更日 | 変更者 | 変更内容 |
|------|--------|--------|---------|
| 1.0 | ★★YYYY-MM-DD | ★★氏名 | 初版作成 |
